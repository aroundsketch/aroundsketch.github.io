
{
  "name": "my_package",
  "description": "",
  "version": "1.0.0",
  "scripts": {
    "dev": "bundle exec jekyll serve --livereload --trace",
    "ruby": "ruby help.rb",
    "git": "git add . && git commit -a -m \"update\" && git push"
  }
}